#include <iostream>

using namespace std;

// create a global variable to hold the list 

int coin_problem_dyn( int amount )
{
	// if the amount is less than 0, it is not possible
	if( amount < 0 )
		return -1;
	// if the amount is 0, then we dont need any coins
	else if( amount == 0 )
		return 0;
	
	// for all other amounts,
	//
}

int main()
{

	return 0;
}
